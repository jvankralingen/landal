# react-scripts 5.0.1 declares a peerOptional on typescript ^3 || ^4 but the
# project uses typescript@^6. npm@7+ rejects this strict-peer mismatch on
# fresh installs (which is what Vercel does). Allowing legacy resolution
# matches what's already pinned in package-lock.json.
legacy-peer-deps=true
